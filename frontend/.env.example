# The base URL of your Django backend API
# For local development, this will typically be:
VITE_API_BASE_URL=http://127.0.0.1:8000/api

# --- API Mocking Control ---
# Set this to "enabled" to use the MSW mock API instead of the live backend.
# Any other value (or leaving it empty) will disable mocking.
# Options: enabled | disabled
VITE_API_MOCKING=disabled